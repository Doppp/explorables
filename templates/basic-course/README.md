# {{COURSE_NAME}}

A new explorable course. Install with `pnpm install`, start with `pnpm course`,
validate with `pnpm validate`, and test with `pnpm test`.

Audience, prerequisites, time estimate, and contribution policy should be
specified before publication. Prose defaults to CC-BY-4.0 and code to MIT.
