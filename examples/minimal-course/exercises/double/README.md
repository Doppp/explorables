# Implement `double`

Complete `starter/double.ts`. Return twice a finite input and throw a
`RangeError` for `NaN` or infinity. Run `pnpm test` from this course directory.

After it passes, explain why the guard belongs at this boundary.
