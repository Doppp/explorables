# Differentiate the graph

Complete `differentiate` for `z=xw+b; y=z²`. Return `y`, `dx`, `dw`, and `db`.
The starter deliberately omits `dz/dw = x`. Explain each local derivative after
the tests pass.
