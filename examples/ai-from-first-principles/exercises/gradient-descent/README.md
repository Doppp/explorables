# Implement one gradient update

Fix `starter/gradient.ts` for `L(θ) = (θ - 3)²`. Validate positive finite
learning rates, then subtract the scaled gradient. Run the command in
`exercise.json`. After it passes, explain the sign and the divergence case.
