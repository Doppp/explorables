# Implement embedding lookup and pair rotation

Complete `embeddingLookup` and `rotatePairs`. Return independent copies of the selected embedding rows, then apply a position-dependent rotation to each coordinate pair. The starter returns aliases into the table and adds the position as a scalar offset.

After the tests pass, explain why rotation preserves vector norm and why actual RoPE applies these rotations to queries and keys.
